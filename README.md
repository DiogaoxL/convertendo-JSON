# Boas-vindas ao Repositório de Exercícios de transformação de dados 👨‍💻

Bem-vindo ao repositório de exercícios de programação em Java! Este repositório foi criado para ajudar no meu aprendizado de conceitos fundamentais de programação orientada a objetos e no uso da biblioteca para manipulação de dados. 

## Enunciados

### Exercício 1: Classe Pessoa com Record

Crie uma classe `Pessoa` usando o conceito de Record em Java, com os seguintes atributos:
- `nome` (String)
- `idade` (int)
- `cidade` (String)

Implemente um programa que utiliza a biblioteca Gson para converter um JSON representando uma pessoa em um objeto do tipo `Pessoa`.

### Exercício 2: Flexibilização da Conversão JSON

Modifique o programa do Exercício 1 para permitir a conversão de um JSON mesmo se alguns campos estiverem ausentes ou se houver campos adicionais não representados no objeto `Pessoa`. Consulte a documentação da biblioteca Gson para flexibilizar a conversão.

### Exercício 3: Classe Livro com Atributos Aninhados

Crie uma classe `Livro` que contenha os seguintes atributos:
- `título` (String)
- `autor` (String)
- `editora` (Editora)

A classe `Editora` deve conter os atributos:
- `nome` (String)
- `endereço` (String)

Implemente um programa que utiliza a biblioteca Gson para converter um JSON aninhado representando um livro em um objeto do tipo `Livro`.

## Principais Aprendizados

Ao concluir esses exercícios, tive esses aprendizados:
- Como utilizar o conceito de Record em Java para criar classes imutáveis.
- Como usar a biblioteca Gson para converter JSON em objetos Java.
- Como flexibilizar a deserialização de JSON para lidar com campos ausentes ou adicionais.
- Como lidar com JSON aninhado e mapear para objetos Java com atributos compostos.

Esse exercícios me ajudaram a fixar e entender melhor os meus aprendizados, espero que te ajude também!
